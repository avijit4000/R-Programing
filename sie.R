# Assuming df is your original dataframe
df <- data.frame(...)  # Replace this with your actual dataframe

# Save rows 10 to 20 in a new dataframe called college
college <- df[10:20, ]

# To confirm, you can print the new dataframe
print(college)
#Replaces substrings by identifying the substrings with str_sub() and assigning them to the results. 
str_sub() <- value 